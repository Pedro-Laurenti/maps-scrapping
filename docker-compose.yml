version: '3'

services:
  gmaps-scraper:
    build:
      context: .
      dockerfile: Dockerfile
    image: gmaps-scraper:latest
    stdin_open: true
    tty: true
    restart: "no"
    volumes:
      - /tmp:/tmp  # Para compartilhar arquivos temporários se necessário
